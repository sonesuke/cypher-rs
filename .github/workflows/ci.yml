name: CI
on: [push, pull_request]
jobs:
  verify:
    name: Build & Verify
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "CI is running and always passing!"
